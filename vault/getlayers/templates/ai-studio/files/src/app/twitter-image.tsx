// Twitter share card — identical to the Open Graph image. Re-exporting keeps a
// single source of truth for the generated artwork.
export {
  default,
  alt,
  size,
  contentType,
} from "@/app/opengraph-image";
