# Local fonts

Drop the hero font files here (prefer `.woff2`; `.ttf`/`.otf` also fine).
Wired via `next/font/local` in `src/app/layout.tsx` and exposed through the
`--font-hero` token (see obsidian/frontend/design-system.md).
