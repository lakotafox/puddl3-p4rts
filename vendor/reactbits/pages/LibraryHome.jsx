// /library — the post-landing state: sidebar (all sections closed, since no
// category owns this route) and a deliberately blank stage.
const LibraryHome = () => <div />;

export default LibraryHome;
